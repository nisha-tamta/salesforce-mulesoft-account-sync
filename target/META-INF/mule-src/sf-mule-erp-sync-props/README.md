# sf-mule-erp-sync-props

Mule project using application.properties keys:
- http.host
- http.port
- api.basePath
- erp.host
- erp.port
- erp.basePath
- sf.username
- sf.password
- sf.token
- sf.loginUrl

Endpoints:
- GET /api/health
- POST /api/sync/accounts
